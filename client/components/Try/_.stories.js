import { action } from '@storybook/addon-actions';
import { Button } from '@storybook/react/demo';
import { storiesOf } from '@storybook/react';

import Try from './'

storiesOf('TEST EIEI', module)
    .add('Try Cmp', () => <Try />)
